/**
 * Catch-all stub for deep `react-native/Libraries/...` subpath imports reached
 * transitively by Paper / safe-area-context during web (jsdom) test runs.
 * Returns a no-op default so codegen helpers evaluate without loading RN source.
 */

const stub = (x?: any): any => x ?? null;
export default stub;
